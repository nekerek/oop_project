# University Information System

IDE-friendly fixed version. Files are sorted by folders and each folder has a proper Java package.

## Run

On Windows:

```bat
run.bat
```

Main class: `controller.MainController`.

Data files are stored in `data`.
